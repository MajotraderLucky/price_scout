---
all:
  children:
    scrapers:
      hosts:
        archbook:
          ansible_host: 192.168.0.10
          ansible_port: 2222
          ansible_user: sergey
          ansible_ssh_private_key_file: ~/.ssh/archbook_key
          ansible_python_interpreter: /usr/bin/python3
          ansible_ssh_extra_args: '-o StrictHostKeyChecking=no -o PasswordAuthentication=no'
          # ansible_become_password: use --ask-become-pass or vault

  vars:
    scraper_path: /home/sergey/price_scout
    dns_output_dir: /tmp/dns_scraper
